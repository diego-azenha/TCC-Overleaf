\definecolor{VioletD}{HTML}{534AB7}
\definecolor{VioletL}{HTML}{EEEDFE}

\begin{tikzpicture}[
    font=\small,
    >={Stealth[length=2.2mm, width=1.5mm]},
    %--- tensor de entrada (cinza, igual a arquitetura.tex)
    tensor/.style={
        draw=gray!55, thick, rounded corners=8pt,
        fill=gray!9, minimum width=3.2cm, minimum height=0.65cm,
        align=center},
    %--- bloco de rede (violeta chamfered, igual a arquitetura.tex)
    net/.style={
        draw=VioletD, very thick, chamfered rectangle,
        chamfered rectangle xsep=0.22cm, fill=VioletL,
        minimum width=3.2cm, minimum height=0.65cm,
        align=center},
    %--- variável latente intermediária (violeta tracejado)
    rv/.style={
        draw=VioletD, thick, dashed, rounded corners=8pt,
        fill=VioletL!50, minimum width=3.2cm, minimum height=0.65cm,
        align=center},
]

%% ── ENTRADAS (fora da caixa, topo) ─────────────────────────
\node[tensor] (e) at (0,   7.6) {Embeddings $e$};
\node[tensor] (y) at (3.8, 7.6) {Retornos futuros $y$};

%% ── FLUXO PRINCIPAL (eixo vertical x=0, de cima para baixo) ─
\node[net] (portlayer) at (0, 6.3)  {Portfolio Layer $\varphi_p$};
\node[rv]  (ap)        at (0, 5.1)  {Pesos dos portfólios $a_p$};

\node[draw=VioletD, very thick, circle,
      minimum size=0.78cm, fill=white, inner sep=0pt, font=\normalsize]
    (otimes) at (0, 4.0) {$\otimes$};

\node[rv]  (yp)       at (0, 2.85) {Retornos dos portfólios $y_p$};
\node[net] (maplayer) at (0, 1.65) {Mapping Layer $\varphi_{\mathrm{map}}$};

%% ── SAÍDA (fora da caixa, base) ─────────────────────────────
\node[rv, fill=VioletL, draw=VioletD, very thick] (zpost) at (0, 0.35)
    {Fatores posteriores $z_{\mathrm{post}}$};

%% ── CAIXA DO MÓDULO ─────────────────────────────────────────
\draw[VioletD, thick, rounded corners=12pt]
    (-2.15, 0.85) rectangle (2.1, 6.85);

%% ── SETAS ───────────────────────────────────────────────────
% Entradas externas (cinza)
\draw[->, gray!60, thick] (e.south)   -- (portlayer.north);
\draw[->, gray!60, thick] (y.south)   |- (otimes.east);
% Fluxo interno (violeta, de cima para baixo)
\draw[->, VioletD, thick] (portlayer.south) -- (ap.north);
\draw[->, VioletD, thick] (ap.south)        -- (otimes.north);
\draw[->, VioletD, thick] (otimes.south)    -- (yp.north);
\draw[->, VioletD, thick] (yp.south)        -- (maplayer.north);
% Saída
\draw[->, VioletD, thick] (maplayer.south)  -- (zpost.north);


\end{tikzpicture}